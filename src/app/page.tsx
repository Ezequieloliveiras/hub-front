'use client';import{useEffect,useState}from'react';import{Alert,Box,Button,Card,CardContent,Chip,MenuItem,Select,Skeleton,Typography}from'@mui/material';import{LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer}from'recharts';import Shell from'@/components/Shell';import{api,money}from'@/lib/api';const labels:{[key:string]:string}={revenue:'Faturamento',profit:'Lucro estimado',margin:'Margem média',orders:'Pedidos',productsSold:'Produtos vendidos',ticket:'Ticket médio',fees:'Taxas',cost:'Custo dos produtos'};export default function Dashboard(){const[d,setD]=useState<any>();useEffect(()=>{api.get('/analytics/dashboard').then(r=>setD(r.data)).catch(()=>setD(null))},[]);return <Shell><div className="page"><Box sx={{display:'flex',justifyContent:'space-between',alignItems:'center',mb:3,flexWrap:'wrap',gap:2}}><Box><Typography variant="h4" fontWeight={800}>Visão geral</Typography><Typography className="muted">Acompanhe o pulso da sua operação.</Typography></Box><Box><Select size="small" defaultValue="7"><MenuItem value="7">Últimos 7 dias</MenuItem><MenuItem value="30">30 dias</MenuItem></Select><Button sx={{ml:1}} variant="outlined">Mercado Livre</Button></Box></Box>{d===undefined?<Skeleton height={360}/>:!d?<Alert severity="info">Conecte seu primeiro marketplace ou use a conta demo para ver seus indicadores.</Alert>:<><div className="grid kpis">{Object.entries(labels).map(([k,l])=><Card className="card" key={k}><CardContent><Typography className="muted" variant="body2">{l}</Typography><Typography variant="h6" fontWeight={750}>{k==='margin'?`${d.kpis[k].toFixed(1)}%`:k==='orders'||k==='productsSold'?d.kpis[k]:money(d.kpis[k])}</Typography><Typography className="positive" variant="caption">↑ 12,4% vs. período anterior</Typography></CardContent></Card>)}</div><Card className="card" sx={{mt:3}}><CardContent><Typography fontWeight={700}>Evolução de faturamento</Typography><Box height={270}><ResponsiveContainer><LineChart data={d.series}><XAxis dataKey="date" hide/><YAxis/><Tooltip formatter={(v)=>money(Number(v))}/><Line type="monotone" dataKey="revenue" stroke="#4f46e5" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></Box></CardContent></Card><Box className="grid" sx={{gridTemplateColumns:{md:'1fr 1fr'},mt:3}}><ProductList title="Produtos mais lucrativos" rows={d.topProducts}/><ProductList title="Produtos com margem baixa" rows={d.lowMargin}/></Box></>}</div></Shell>}function ProductList({title,rows}:{title:string;rows:any[]}){return <Card className="card"><CardContent><Typography fontWeight={700} mb={1}>{title}</Typography>{rows.length?rows.map(x=><Box key={x.sku} sx={{py:1.3,display:'flex',justifyContent:'space-between',borderBottom:1,borderColor:'divider'}}><Box><Typography fontSize={14} fontWeight={600}>{x.name}</Typography><Typography className="muted" variant="caption">{x.sku} · {x.quantity} vendas</Typography></Box><Chip size="small" label={`${x.margin.toFixed(1)}%` }color={x.margin<10?'warning':'success'}/></Box>):<Typography className="muted">Nenhum produto nesta categoria.</Typography>}</CardContent></Card>}
