'use client';import{useEffect,useState}from'react';import{Alert,Card,CardContent,Typography}from'@mui/material';import Shell from'@/components/Shell';import{api,money}from'@/lib/api';export default function Dre(){const[d,setD]=useState<any>();useEffect(()=>{api.get('/analytics/dashboard').then(r=>setD(r.data))},[]);const k=d?.kpis||{};const rows=[['Receita bruta',k.revenue],['(-) Taxas dos marketplaces',-k.fees],['(-) Custo dos produtos',-k.cost],['= Lucro estimado',k.profit]];return <Shell><div className="page"><Typography variant="h4" fontWeight={800} mb={2}>DRE</Typography><Alert severity="info" sx={{mb:2}}>DRE gerencial estimada — não é um documento contábil oficial.</Alert><Card><CardContent>{rows.map(([l,v]:any)=><div key={l} style={{display:'flex',justifyContent:'space-between',padding:'15px 5px',borderBottom:'1px solid #eee'}}><b>{l}</b><b>{money(v)}</b></div>)}</CardContent></Card></div></Shell>}
