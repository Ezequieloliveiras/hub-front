# Seller Pulse Web

```bash
cp .env.example .env.local
npm install
npm run dev
```

Abra `http://localhost:3000`. A API deve estar em `http://localhost:3001/api`. A conta demo é `demo@sellerpulse.com` / `demo1234` após executar o seed da API.
# hub-front
